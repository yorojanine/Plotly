Plotly.newPlot("plotArea", [{x: [1, 2, 3], y: [10, 20, 30]}]);
// Do you need to create a new js for each Plotly? It won't call this new plot unless this first block was commented out.

Plotly.newPlot("plotTesting", [{x: [5, 10, 15], y: [10, 20, 30]}]);

